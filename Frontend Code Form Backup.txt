<!-- Google Font -->
<link
  href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap"
  rel="stylesheet"
/>

<style>
  body {
    font-family: "Montserrat", sans-serif;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
  }

  .form-wrapper {
    display: flex;
    max-width: 900px;
    width: 100%;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
    animation: fadeIn 1s ease;
    background: #fff;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .form-side {
    flex: 1;
    background: #fff;
    padding: 40px;
  }

  .form-side h3 {
    font-weight: 600;
    margin-bottom: 10px;
    color: #2e7d32;
    text-align: center;
  }

  .form-side p {
    text-align: center;
    font-size: 14px;
    color: #555;
    margin-bottom: 30px;
  }

  .form-group {
    position: relative;
    margin: 20px 0;
  }

  .form-input {
    width: 100%;
    padding: 14px 12px;
    border: 2px solid #e0e0e0;
    border-radius: 12px;
    background: #f9f9f9;
    font-size: 14px;
    outline: none;
    transition: 0.3s ease;
    color: #000;
  }

  /* Dark placeholder styles */
  .form-input::placeholder,
  .form-input::-webkit-input-placeholder,
  .form-input::-moz-placeholder,
  .form-input:-ms-input-placeholder,
  .form-input:-moz-placeholder {
    color: #000;
    opacity: 1;
  }

  .form-input:focus {
    border-color: #2e7d32;
    background: #fff;
    box-shadow: 0 0 10px rgba(46, 125, 50, 0.2);
  }

  .form-label {
    position: absolute;
    top: 16px;
    left: 14px;
    font-size: 14px;
    color: #999;
    pointer-events: none;
    transition: 0.2s ease all;
  }

  #headline.form-input {
    height: 65px;
    padding-top: 18px;
    font-size: 14px;
    white-space: normal;
    line-height: 1.4;
  }

  .form-input:focus ~ .form-label,
  .form-input:not(:placeholder-shown) ~ .form-label {
    top: -8px;
    left: 10px;
    font-size: 12px;
    color: #2e7d32;
    background: #fff;
    padding: 0 5px;
    border-radius: 4px;
  }

  .error-msg {
    font-size: 12px;
    color: #d32f2f;
    margin-top: 4px;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .error-msg.active {
    opacity: 1;
  }

  .submit-btn {
    width: 100%;
    padding: 14px;
    border: none;
    border-radius: 14px;
    font-weight: 600;
    font-size: 16px;
    background: linear-gradient(135deg, #2e7d32, #66bb6a);
    color: white;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 18px rgba(46, 125, 50, 0.3);
  }

  #statusMsg {
    text-align: center;
    margin-top: 15px;
    font-weight: 500;
    color: #2e7d32;
  }

  .info-side {
    flex: 1;
    background: url("https://images.unsplash.com/photo-1569982175971-d92b01cf8694?q=80&w=735&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D")
      center/cover no-repeat;
    color: white;
    padding: 50px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    position: relative;
  }

  .info-side::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.2);
  }

  .info-side h2,
  .info-side p {
    position: relative;
    z-index: 1;
  }

  .info-side h2 {
    font-size: 28px;
    margin-bottom: 15px;
    animation: slideIn 1s ease;
  }

  .info-side p {
    font-size: 14px;
    line-height: 1.6;
    max-width: 300px;
    margin-bottom: 25px;
  }

  .step {
    display: none;
  }

  .step.active {
    display: block;
  }

  #email-note {
    text-align: center;
    font-size: 14px;
    color: #555;
    margin-bottom: 20px;
    display: none;
  }

  /* ---------- MOBILE STYLES ---------- */
  @media (max-width: 768px) {
    .form-wrapper {
      flex-direction: column;
      box-shadow: none;
      border-radius: 20px;
    }

    .form-side {
      padding: 24px 20px;
    }

    .form-input {
      font-size: 15px;
      padding: 12px 10px;
    }

    .form-label {
      font-size: 13px;
    }

    #headline.form-input {
      height: auto;
      min-height: 60px;
    }

    .submit-btn {
      font-size: 15px;
      padding: 12px;
    }

    .info-side {
      display: none;
    }

    .form-side h3 {
      font-size: 20px;
    }

    .form-side p {
      font-size: 13px;
      margin-bottom: 20px;
    }
  }
</style>

<div class="form-wrapper">
  <!-- Left Form -->
  <div class="form-side">
    <h3>Ask Repleaf AI ✨</h3>
    <p>
      Confused? Tell AI about you and get a personalized email to see how
      Repleaf helps you.
    </p>
    <form id="repleafForm" novalidate>
      <!-- Step 1 -->
      <div id="step1" class="step active">
        <div class="form-group">
          <input
            type="text"
            id="firstName"
            class="form-input"
            placeholder=" "
            required
          />
          <label class="form-label" for="firstName">First Name</label>
          <div class="error-msg">First name is required</div>
        </div>
        <div class="form-group">
          <input
            type="text"
            id="lastName"
            class="form-input"
            placeholder=" "
            required
          />
          <label class="form-label" for="lastName">Last Name</label>
          <div class="error-msg">Last name is required</div>
        </div>
        <div class="form-group">
          <input
            type="text"
            id="headline"
            class="form-input"
            placeholder=" "
            required
          />
          <label class="form-label" for="headline"
            >Work Headline (e.g., Software Developer at Google)</label
          >
          <div class="error-msg">Headline is required</div>
        </div>
        <button type="button" class="submit-btn" id="nextBtn">Ask AI🔥</button>
      </div>

      <!-- Step 2 -->
      <div id="step2" class="step">
        <div class="form-group">
          <input
            type="email"
            id="email"
            class="form-input"
            placeholder=" "
            required
          />
          <label class="form-label" for="email">Email</label>
          <div class="error-msg">Valid email is required</div>
        </div>
        <button type="submit" class="submit-btn">Get it on email ✨</button>
      </div>
    </form>
    <p id="statusMsg"></p>
  </div>

  <!-- Right Info -->
  <div class="info-side">
    <h2>Repleaf</h2>
    <p>
      The only platform built to showcase your real professional credibility
      through your dynamic Repleaf Score. Join us and unlock a smarter future of
      work.
    </p>
  </div>
</div>

<script>
  const form = document.getElementById("repleafForm");
  const statusMsg = document.getElementById("statusMsg");
  const step1 = document.getElementById("step1");
  const step2 = document.getElementById("step2");
  const nextBtn = document.getElementById("nextBtn");
  const emailNote = document.getElementById("email-note");

  nextBtn.addEventListener("click", () => {
    let isValid = true;
    step1.querySelectorAll(".form-group").forEach((group) => {
      const input = group.querySelector(".form-input");
      const error = group.querySelector(".error-msg");
      if (!input.value.trim()) {
        error.classList.add("active");
        isValid = false;
      } else {
        error.classList.remove("active");
      }
    });
    if (!isValid) return;

    step1.classList.remove("active");
    step2.classList.add("active");
    emailNote.style.display = "block";
  });

  form.addEventListener("submit", async function (e) {
    e.preventDefault();
    let isValid = true;
    step2.querySelectorAll(".form-group").forEach((group) => {
      const input = group.querySelector(".form-input");
      const error = group.querySelector(".error-msg");
      if (
        !input.value.trim() ||
        (input.type === "email" && !/\S+@\S+\.\S+/.test(input.value))
      ) {
        error.classList.add("active");
        isValid = false;
      } else {
        error.classList.remove("active");
      }
    });
    if (!isValid) return;

    statusMsg.innerText = "Let AI write its magic...";
    try {
      const response = await fetch(
        "https://repleaf-website-backend.vercel.app/api/send-email",
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            firstName: document.getElementById("firstName").value,
            lastName: document.getElementById("lastName").value,
            email: document.getElementById("email").value,
            headline: document.getElementById("headline").value,
          }),
        }
      );
      const result = await response.json();
      if (result.success) {
        statusMsg.innerText =
          "✅ Done. Check your email for your personalized Repleaf guide!";
      } else {
        statusMsg.innerText = "❌ Something went wrong. Please try again.";
      }
    } catch (err) {
      console.error(err);
      statusMsg.innerText = "⚠️ Server error. Please try again later.";
    }
  });
</script>
